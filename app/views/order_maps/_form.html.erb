<%= form_with(model: order_map) do |form| %>
  <% if order_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_map.errors.count, "error") %> prohibited this order_map from being saved:</h2>

      <ul>
        <% order_map.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :words %>
    <%= form.text_field :words %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
